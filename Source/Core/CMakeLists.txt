file(GLOB SOURCES *.cpp)
file(GLOB HEADERS *.h)
add_pch(Precompiled.h Precompiled.cpp ${SOURCES})
add_library(Core SHARED ${SOURCES} ${HEADERS})
add_definitions(-DBUILDING_CORE)
target_link_libraries(Core ${TMXPARSER_LIBS})