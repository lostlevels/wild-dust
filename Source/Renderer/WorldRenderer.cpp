#include "Precompiled.h"
#include "WorldRenderer.h"
#include "Texture.h"
#include "Shader.h"
#include "SpriteBatcher.h"
#include "Font.h"
#include "Core/World.h"
//#include "GUI.h"

void WorldRenderer::drawWorld(World *world, RendererBase *renderer) {
//	for (auto &kv : world->mEntities) {
//		auto entity = kv.second;
//		auto textureFile = entity->getRepresentation().mImageFilename;
//
//		if (!textureFile.empty()) {
//			auto texture = renderer->getTexture(textureFile);
//			auto batcher = renderer->getSpriteBatcher(texture, BLEND_ALPHA);
//		}
//	}
}
