file(GLOB SOURCES *.cpp)
file(GLOB HEADERS *.h)
add_pch(Precompiled.h Precompiled.cpp ${SOURCES})
add_definitions(-DBUILDING_SERIALIZATION)
add_library(Serialization SHARED ${SOURCES} ${HEADERS})
target_link_libraries(Serialization Game Core)